# 1. Base Python image jisme python pre-installed hai
FROM python:3.10-slim

# 2. Working directory set karna
WORKDIR /app

# 3. System dependencies install karna
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

# 4. Requirements copy aur install karna
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 5. Saara code container mein copy karna
COPY . .

# 6. Port expose karna jo Render use karega
EXPOSE 5000

# 7. Main command app chalane ke liye (Status 127 bypassed)
CMD ["python", "bot.py"]
